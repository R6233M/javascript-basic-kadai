// 変数の宣言・値の代入
let bodyWeight = 68;

// 変数の宣言・値の代入
let bodyHeight = 1.7;

// 変数の宣言・値の代入
let bodyMassIndex = bodyWeight / (bodyHeight * bodyHeight);

// コンソールへの出力
console.log(bodyWeight / (bodyHeight * bodyHeight));